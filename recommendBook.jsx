import React from "react";

const RecommendBook =({result})=>{
    return(
        <div className={`bookInfo ${props.color}`}>
        //タイトル、画像、作者名とか受け渡す CSSを付け加える
        </div>
    );
}

export default RecommendBook;